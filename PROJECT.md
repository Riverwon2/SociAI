# SociAI — 30분 교환소

이동에 제약이 있는 1인 가구가 당일 필요한 무료·비의료 생활지원을 한 번만 요청하면, 요청을 안전한 태스크로 나누고 적합한 이웃을 순서대로 섭외해 30분 이내의 매칭 결과를 만드는 운영 에이전트다.

이 프로젝트의 핵심은 후보를 추천하는 데서 끝나지 않고, 후보의 거절이나 무응답을 관찰해 사전 승인된 범위 안에서 다음 행동까지 실행하는 것이다.

## 문제 정의

고령, 장애, 일시적 부상 등으로 이동이나 물건 운반이 어려운 1인 가구에는 생필품 수령, 분리배출, 서류 전달처럼 30분 안에 끝낼 수 있지만 당일 해결해야 하는 생활 과업이 반복적으로 발생한다.

이런 요청은 공적 돌봄 서비스를 신청하기에는 너무 작고 긴급하며, 가족이나 지인에게 계속 부탁하기에는 심리적 부담과 관계 의존성이 크다. 지역에는 짧은 시간 동안 도움을 줄 의사가 있는 주민이 있어도 요청의 범위와 안전성을 확인하고, 적합한 사람을 찾아 수락 여부를 확인하며, 거절이나 무응답이 발생했을 때 다음 대안을 실행하는 체계가 부족하다.

그 결과 사소해 보이는 생활 문제가 식사 누락, 주거 위생 저하, 무리한 외출, 사회적 고립으로 이어질 수 있다. 30분 교환소는 비용이 발생하지 않는 비의료 생활지원만을 대상으로 요청자가 한 번 제출한 시간·지역·활동 범위 안에서 도움 제공 후보를 찾고 각 후보의 자발적인 수락을 확인한다.

> 이동에 제약이 있는 1인 가구는 당일 발생한 30분 이내의 비의료 생활지원을 안전하고 부담 없이 요청하기 어렵고, 지역의 도움 가능 주민도 요청의 범위와 안전성을 확인한 뒤 자발적으로 참여할 수 있는 연결 체계가 없어 필요한 도움과 지역의 유휴 시간이 실제 행동으로 이어지지 못하고 있다.

서비스가 검증할 핵심 질문은 다음과 같다.

> 요청자의 한 번의 입력만으로 필요한 일을 안전한 작업과 제외해야 할 작업으로 나누고, 사전에 동의된 시간·거리·활동 범위에 맞는 이웃을 순서대로 찾아 수락 여부를 확인하며, 거절이나 무응답에도 다음 대안을 실행해 안전한 매칭 결과를 만들 수 있는가?

여기서 한 번의 입력은 요청자에게 추가 질문을 하지 않는다는 뜻이다. 후보의 응답은 별도의 요청자 입력이 아니라 시스템 이벤트이며, 누구도 강제로 매칭하지 않는다.

## 제품 원칙

- 요청자는 최초 제출 이후 최종 결과까지 추가 질문을 받지 않는다.
- 후보의 수락·거절·무응답은 시스템이 관찰하는 응답 이벤트다.
- 위험한 태스크만 제외하고 안전한 태스크는 계속 진행한다.
- 모든 후보와 허용된 대안을 소진하면 `unmatched`를 반환한다.
- 데모에는 가상 인물, 지역, 연락처와 응답 이력만 사용한다.

지원하는 최종 상태는 다음과 같다.

- `fully_matched`
- `partially_matched`
- `safety_excluded`
- `unmatched`

## MVP 범위

### 구현 대상

- 필수 선택 사항과 자연어 설명을 한 번에 받는 `InitialRequest`
- 실제 OpenAI API 또는 공식 SDK를 사용한 요청 해석과 태스크 분해
- 태스크별 결정론적 안전 판정과 정보 충분성 검사
- 지역, 가용 시간, 거리, 경험, 신뢰도 기반 후보 필터링과 랭킹
- 결정론적 후보 응답 시뮬레이터와 가상 시계
- 거절·무응답 후 최대 3명까지 자동 재섭외
- 안전한 태스크의 부분 성공을 보존하는 결과 집계
- 정규화 이벤트 화면과 별도의 raw 도구 이벤트 화면
- 세 대표 시나리오의 라이브 데모와 재현 가능한 테스트

### 제외 대상

- 유료 요청, 실제 결제와 외부 정산
- 실제 메시징과 연락처 교환
- 실제 지도 제공자 연동
- 실제 신원 확인과 기관 배차
- 의료행위, 현금 대리, 아동 단독 돌봄의 일반 이웃 매칭
- 시간 크레딧, 후기, 공개 성공 사례의 실제 운영

제외 기능을 화면에 표현할 때는 `simulation`으로 명확히 표시한다. Replay 데이터를 라이브 OpenAI raw 실행으로 표현하지 않는다.

## 최초 입력

`InitialRequest`는 다음 정보를 한 번에 받는다.

| 항목           | 규칙                                       |
| -------------- | ------------------------------------------ |
| 도움 내용      | 자연어, 필수                               |
| 희망 시간      | 당일 시간 범위, 필수                       |
| 활동 지역      | 대략적인 지역, 필수                        |
| 최대 활동 시간 | 30분 이하, 필수                            |
| 시간 조정      | 사전 허용 여부                             |
| 부분 완료      | 사전 허용 여부                             |
| 범위 축소      | 사전 허용 여부                             |
| 비용           | `none`으로 고정                            |
| 결제 방식      | `not_applicable`로 고정                    |
| 추가 사항      | 접근성, 물품 무게, 실내 출입 여부 등, 선택 |

필수 값이 유효하지 않거나 무료·비의료·30분 범위를 벗어나면 중간 질문 없이 안전한 최종 결과를 반환한다. 정확한 주소와 연락처는 매칭 확정 전에 후보에게 전달하지 않는다.

## 실행 파이프라인

```text
InitialRequest
  │
  ├─ 1. 경계 입력 검증
  ├─ 2. OpenAI 요청 해석·태스크 분해
  ├─ 3. 태스크별 결정론적 안전 판정
  ├─ 4. 태스크별 정보 충분성 검사
  ├─ 5. 후보 필터링·랭킹
  ├─ 6. 최고 순위 후보에게 섭외
  ├─ 7. 결정론적 응답 이벤트 관찰
  ├─ 8. 실패 원인에 따른 계획 갱신
  ├─ 9. 매칭 확정과 태스크 결과 집계
  └─ 10. 요청 최종 결과 생성
```

OpenAI는 해석, 계획·재계획, 사용자 안내 문구를 담당한다. 안전 정책, 정보 충분성, 점수, 타이머, 상태 전이, 후보 응답은 결정론적 코드가 담당한다.

후보 점수는 다음 공식을 사용하고 각 구성 요소를 UI에 표시한다.

```text
총점 = 가용성 40% + 거리 25% + 관련 경험 20% + 신뢰도 15%
```

같은 입력과 seed는 같은 후보 순서를 만든다. 동점은 `candidateId` 오름차순으로 처리한다. 후보당 가상 10분을 기다리며 최대 3명까지만 시도한다.

실패할 때는 한 번에 하나의 변수만 변경한다.

1. 다음 후보
2. 사전 허용된 시간 조정
3. 사전 허용된 범위 축소

## 안전과 fallback 정책

`SafetyLevel`과 `SafetyAction`은 별도 필드로 유지한다. 정보 부족은 위험 수준이 아니라 `SufficiencyDecision`으로 표현한다.

| 안전 수준     | 행동                                     |
| ------------- | ---------------------------------------- |
| `low`         | 일반 후보 탐색 진행                      |
| `conditional` | 결정론적으로 조건을 확인한 경우에만 진행 |
| `high`        | 해당 태스크의 일반 이웃 매칭 차단        |
| `emergency`   | 해당 태스크의 매칭 중단과 긴급 안내      |

의료행위, 요청자를 대신한 현금 취급, 아동 단독 돌봄은 일반 이웃 매칭 대상으로 취급하지 않는다. 위험한 태스크가 섞여 있어도 관련 없는 안전한 태스크의 진행과 성공을 취소하지 않는다.

`fallbackPolicy`가 허용하지 않은 변경을 위해 사용자에게 질문하지 않는다. 후보와 허용된 fallback을 모두 소진하면 해당 태스크를 `unmatched`로 종료한다. 정보가 부족할 때 사실을 만들어내지 않고 영향받은 태스크만 보류한다.

요청 설명과 모든 도구 결과는 신뢰할 수 없는 데이터로 취급한다. 프롬프트에는 prompt injection, 모순된 입력, 누락 정보, 빈 후보군, 도구 오류와 잘못된 구조화 출력에 대한 처리 규칙을 포함한다. 모델 출력은 스키마 검증을 통과한 경우에만 다음 단계로 전달한다.

## 프로젝트 구조와 소유권

```text
SociAI/
├─ apps/
│  ├─ server/                    # 1번: AI·오케스트레이션
│  └─ web/                       # 3번: 프론트엔드·데모
├─ packages/
│  ├─ contracts/                # 3번: 공용 Zod 계약
│  ├─ decisions/                # 2번: 안전·충분성·랭킹·정책
│  ├─ simulator/                # 2번: 시나리오·가상 시계·합성 데이터
│  └─ event-stream/             # 3번: 이벤트 계약·전송 유틸
└─ tests/e2e/                   # 3번 주도: 대표 시나리오 검증
```

| 인원 | 역할              | 주요 책임                                                              |
| ---- | ----------------- | ---------------------------------------------------------------------- |
| 1번  | AI·오케스트레이션 | OpenAI 연동, 실행 계획, 상태 관리, 재계획, 결과 집계, 서버 이벤트 발행 |
| 2번  | 결정·검증         | 안전 정책, 정보 충분성, 후보 랭킹, 시뮬레이터, 가상 시계, 단위 테스트  |
| 3번  | 통합·프론트엔드   | 공용 계약, 이벤트 전송 유틸, UI, E2E, 라이브 데모                      |

### 1번 — AI·오케스트레이션

소유 영역은 `apps/server`다.

- 서버 실행 환경과 요청 API
- 실제 OpenAI API·SDK 연결
- 자연어 요청 해석과 태스크 분해
- run/task 상태, 실행 상태 머신과 도구 디스패치
- 2번이 만든 결정 도구 호출
- 거절·타임아웃·안전 차단·도구 실패 후 재계획
- `plan.updated` 생성
- 태스크별 결과와 최종 결과 집계
- 서버 런타임의 `AgentEvent`와 `RawToolEvent` 발행
- OpenAI 통합 테스트, 프롬프트 eval과 이벤트 순서 검증

1번은 결정 정책을 다시 구현하거나 프론트용 타입을 별도로 만들지 않는다. 이벤트는 서버가 발행하지만 3번이 관리하는 계약과 전송 유틸을 따른다.

### 2번 — 결정·검증

소유 영역은 `packages/decisions`와 `packages/simulator`다.

- 합성 요청자·후보 데이터
- 태스크별 안전 판정과 정보 충분성 검사
- 후보 필터와 40/25/20/15 점수
- fallback과 최대 3명 재시도 정책
- 고정 seed와 가상 시계
- 수락·거절·무응답 시뮬레이터
- 안전·점수·재시도·결정론 단위 테스트
- 커버리지와 오류 시나리오 관리

2번의 구현은 서버 없이 실행 가능한 순수 함수로 제공한다.

### 3번 — 통합·프론트엔드·데모

소유 영역은 `packages/contracts`, `packages/event-stream`, `apps/web`, `tests/e2e`다.

- 공용 Zod 계약, 예제 JSON과 계약 테스트
- 실행 요청 API와 프론트엔드 연결
- `AgentEvent`와 `RawToolEvent` 분리 계약·전송 유틸
- `sequence`와 `toolCallId` 상관관계
- 이벤트 저장·조회, 스트림 재연결과 replay 연결
- raw provider payload 무변형 보존
- one-shot 요청 화면
- 작업·후보·안전 상태와 후보 점수 근거 표시
- `plan.updated` 타임라인과 raw 이벤트 전용 두 번째 화면
- Replay, live와 `simulation` 상태 구분
- 세 대표 시나리오 E2E
- 발표와 라이브 데모 운영

계약의 기술적 편집자는 3번이지만 계약 변경은 세 명 모두 승인한다.

의존 방향은 단방향으로 유지한다.

```text
apps/server  ──→ contracts, decisions, simulator, event-stream
apps/web     ──→ contracts
decisions    ──→ contracts
simulator    ──→ contracts
event-stream ──→ contracts
```

`packages/*`는 `apps/*`를 import하지 않는다. `decisions`, `simulator`, `event-stream`은 서로 직접 의존하지 않고 서버 오케스트레이터가 조합한다.

## 담당자 간 인터페이스

| 제공자 | 소비자 | 계약                                                             |
| ------ | ------ | ---------------------------------------------------------------- |
| 3번    | 1번    | 검증된 `InitialRequest`와 공용 스키마                            |
| 1번    | 2번    | `Task`, 후보 목록, 시나리오와 seed                               |
| 2번    | 1번    | `SafetyDecision`, `SufficiencyDecision`, 후보 순위와 응답 이벤트 |
| 1번    | 3번    | `AgentEvent`, `RawToolEvent`, `FinalResult`                      |

2번이 제공할 최소 함수 경계는 다음과 같다.

```text
checkSafety(task) -> SafetyDecision
checkSufficiency(task, availableFacts) -> SufficiencyDecision
rankCandidates(task, candidates) -> RankedCandidate[]
simulateOutreach(scenario, candidate, seed, clock) -> ResponseEvent
```

태스크 결과 집계는 1번 서버가 담당하며 `aggregateTaskResults(results) -> FinalResult` 형태의 공용 계약을 사용한다.

다른 담당자의 소유 파일을 대규모 수정하지 않고 인터페이스 변경으로 요청한다. 공용 설정과 계약 파일을 수정하기 전에는 팀에 알린다.

## 공용 계약

`packages/contracts`를 TypeScript 타입과 런타임 검증의 유일한 원본으로 사용한다. Zod 스키마에서 정적 타입을 파생한다.

먼저 확정할 계약은 다음과 같다.

- `InitialRequest`
- `Task`
- `SafetyDecision`
- `SufficiencyDecision`
- `Candidate`
- `AgentEvent`
- `RawToolEvent`
- `FinalResult`

모든 관련 레코드는 `runId`, `requestId`, 선택적 `taskId`, 단조 증가하는 `sequence`, `schemaVersion`을 일관되게 사용한다. 도구 호출과 결과는 같은 `toolCallId`로 연결한다.

계약은 세 명이 함께 승인하며, 계약 예제와 계약 테스트도 같은 변경에서 갱신한다.

계약 변경 규칙:

- 모든 API와 도구 경계에서 런타임 검증을 수행한다.
- 이벤트는 append-only로 유지한다.
- UI는 시각이 아니라 `sequence`로 정렬한다.
- 필드를 삭제하거나 이름을 바꾸지 않고 optional 필드 추가를 우선한다.
- 호환 불가능한 변경은 `schemaVersion`을 증가시킨다.
- 예제 JSON과 계약 테스트를 같은 변경에서 수정한다.
- 알 수 없는 이벤트가 프론트엔드를 중단시키지 않게 처리한다.

## 이벤트와 관측성

두 이벤트 스트림은 분리한다.

### `AgentEvent`

주 화면에서 사용하는 정규화된 이벤트다.

```text
request.created
plan.created
task.created
safety.checked
sufficiency.checked
candidates.ranked
outreach.sent
neighbor.replied / outreach.timed_out
plan.updated
match.confirmed / task.blocked
tool.failed
request.completed
```

모든 `plan.updated`는 `revision`, `trigger`, `observation`, `previousAction`, `nextAction`, `policyApplied`, `userInputRequired: false`를 포함한다. 실패 후 다음 행동보다 먼저 발행한다.

### `RawToolEvent`

심사위원용 두 번째 화면에서 사용하는 실제 OpenAI SDK 이벤트다. Provider payload 전체를 `raw`에 그대로 보존하고 실행 메타데이터는 `raw` 밖에 둔다. 가짜 raw 로그를 만들지 않는다.

서버는 런타임 이벤트를 발행하고, `packages/event-stream`은 공용 계약에 맞는 검증·전송 유틸과 raw 보존 규칙을 제공한다. UI는 시각이 아니라 `sequence`로 이벤트를 정렬하며 알 수 없는 이벤트도 안전하게 표시한다.

이벤트 스트림 재연결 시에는 마지막으로 수신한 `sequence` 다음부터 재개한다. Replay는 live와 같은 정규화 이벤트 스키마를 사용하되, replay raw 데이터를 라이브 OpenAI 로그로 주장하지 않는다.

## 대표 데모 시나리오

아래 인물, 지역과 응답 이력은 모두 데모용 가상 데이터다.

### 시나리오 1: 생필품 수령

- 요청자: 다리 부상으로 2주간 외출이 어려운 67세 1인 가구
- 요청: 오늘 오후 6시 전, 도보 10분 거리 관리실의 가벼운 생필품 상자를 현관 앞까지 전달
- 조건: 최대 30분, 비용 없음, 무거운 물건 없음
- 에이전트 행동: 안전한 생활지원으로 분류하고 거리·시간·경험·신뢰도로 후보를 정렬한 뒤 첫 후보에게 요청
- 후보 응답: 첫 후보 수락
- 최종 결과: `fully_matched`
- 데모 포인트: 한 번의 요청이 안전 판단, 후보 선정과 수락 확인을 거쳐 매칭되는 기본 흐름

### 시나리오 2: 분리배출

- 요청자: 휠체어를 사용하며 혼자 거주하는 54세 1인 가구
- 요청: 오늘 오후 8시 전, 현관 앞의 가벼운 재활용품을 단지 분리배출장까지 이동
- 조건: 최대 20분, 비용 없음, 실내 출입 없음
- 에이전트 행동: 후보를 순위대로 섭외하고 각 후보에게 최대 10분의 가상 응답 시간 부여
- 후보 응답: 첫 후보 거절 → 두 번째 후보 무응답 → `plan.updated` → 세 번째 후보 수락
- 최종 결과: `fully_matched`
- 데모 포인트: 요청자에게 다시 묻지 않으면서 후보의 선택권을 보장하고 실패에 맞춰 계획을 변경하는 적응 과정

### 시나리오 3: 문서 전달과 약 복용 보조

- 요청자: 일시적인 허리 부상으로 거동이 불편한 72세 1인 가구
- 요청: 현관 앞 문서봉투를 도보 8분 거리 주민센터 민원함에 전달하고 처방약 복용도 보조
- 조건: 오늘 오후 5시 전, 최대 30분, 비용 없음
- 에이전트 행동: 문서 전달과 약 복용 보조로 분해하고, 의료행위 가능성이 있는 약 복용 보조는 차단하되 문서 전달은 계속 진행
- 후보 응답: 안전한 문서 전달 태스크의 첫 후보 수락
- 최종 결과: 문서 전달 `fully_matched`, 약 복용 보조 `safety_excluded`, 전체 `partially_matched`
- 데모 포인트: 위험한 작업 때문에 전체 요청을 취소하지 않고 안전한 도움만 성사시키는 부분 성공

세 시나리오는 동일한 계약을 사용하는 가상 입력, 후보 목록, 응답 순서, 예상 이벤트 순서와 최종 결과 fixture를 공유한다.

## 실제 실행과 simulation 경계

실제로 동작해야 하는 범위:

- 입력 검증
- OpenAI 요청 해석과 태스크 분해
- 도구 호출과 결과 수신
- 안전·랭킹·타이머 코드
- 거절·타임아웃 후 재계획
- 두 이벤트 스트림의 실시간 전송
- 최종 결과 집계

Simulation으로 제공하는 범위:

- 요청자와 이웃 프로필
- 후보 응답과 메시지 전달
- 가상 시계
- 채팅, 지도, 크레딧과 후기 상태

Simulation 어댑터는 미래의 실제 어댑터와 같은 계약을 사용한다.

## 실패 처리

| 실패                    | 처리                                                             |
| ----------------------- | ---------------------------------------------------------------- |
| OpenAI 호출 실패        | 실제 오류 이벤트 발행, 완료된 태스크 보존, 안전한 최종 상태 반환 |
| OpenAI 구조화 출력 오류 | 스키마 검증 후 최대 1회 재시도, 다시 실패하면 안전 종료          |
| 안전 도구 오류          | 영향받은 태스크만 중단하고 다른 태스크 계속 진행                 |
| 후보 없음               | 허용된 fallback 실행 또는 `unmatched`                            |
| 후보 거절               | `plan.updated` 후 다음 후보                                      |
| 후보 무응답             | 가상 10분 후 timeout, `plan.updated` 후 다음 후보                |
| 이벤트 스트림 재연결    | 마지막 `sequence` 다음부터 재개                                  |
| 알 수 없는 이벤트       | UI에서 unknown 상태로 표시하고 실행 지속                         |

오류 시 준비된 replay를 사용할 수 있지만 반드시 replay로 표시한다. 도구 실패는 이벤트로 남기고, 이미 완료된 태스크를 보존하며, 제한된 fallback 또는 안전한 최종 상태를 선택한다. 후보가 없을 때 제공자나 기관 연계를 지어내지 않는다.

## 병렬 개발 순서

### T+0~1h: 착수와 계약 v0 동결

- README, package manifest, lockfile, CI, 기존 계약·테스트와 Git 상태 확인
- 세 대표 fixture와 결과 상태 확정
- 공용 계약, 이벤트 이름, ID와 `sequence` 규칙 확정
- 실제 실행과 simulation 경계 확정

T+1h 이후에는 optional 필드 추가를 우선한다. 필드 삭제나 이름 변경은 세 명의 합의와 전체 소비자 수정을 요구한다.

### T+1~5h: 독립 병렬 구현

- 1번은 typed stub으로 오케스트레이션 작성
- 2번은 순수 함수와 단위 테스트 작성
- 3번은 계약-valid fixture로 두 이벤트 화면과 결과 UI 작성

### T+5~8h: 시나리오 1 수직 통합

- Stub을 실제 결정 함수로 교체
- Fixture 이벤트를 실제 스트림으로 교체
- 실제 OpenAI 호출과 raw 화면 확인
- 시나리오 1을 10회 반복

### T+8~13h: 시나리오 2 통합

- 거절·타임아웃과 가상 시계 연결
- `plan.updated`의 선행 순서 검증
- 세 번째 후보 수락까지 연결

### T+13~16h: 시나리오 3 통합

- 혼합 요청의 태스크 분해
- 의료 가능 태스크 차단
- 안전 태스크 계속 진행
- 부분 결과 집계

### T+16~20h: 전체 검증

- Format, lint, typecheck, unit, integration, E2E와 build
- 커버리지, 의존성 audit, secret scan과 `git diff --check`
- Jailbreak, 희소 입력, 잘못된 도구 출력, 후보 없음과 도구 실패 eval

### T+20~24h: 동결과 리허설

- 핵심 기능 동결
- 두 화면을 사용하는 라이브 데모 반복
- 네트워크·API 오류 시 안전 종료 확인
- 발표 역할과 실제 실행·simulation 경계 설명 연습

공용 계약과 설정 파일을 수정하기 전에는 팀에 알린다. 다른 담당자의 영역을 직접 다시 구현하지 않고 계약을 통해 연결한다.

병렬 구현의 시작 조건은 공용 계약, 세 시나리오 fixture와 예상 이벤트 순서가 확정된 상태다. 마지막에 한 번 합치지 않고 2~3시간 간격으로 통합한다.

## 테스트 소유권과 검증 범위

### 1번

- OpenAI tool calling 통합 테스트와 프롬프트 eval
- 요청부터 최종 결과까지 이벤트 순서
- `plan.updated`가 다음 행동보다 먼저 발생하는지 검증
- 부분 성공 보존과 도구 실패 처리

### 2번

- 안전 규칙과 정보 충분성
- 후보 점수 구성과 동점 처리
- 가상 시계, timeout과 최대 3명 정책
- 같은 seed의 동일 결과

### 3번

- 모든 계약과 예제 JSON
- API·도구·이벤트 경계의 계약 테스트
- `toolCallId` call/result 상관관계
- raw payload 무변형
- 알 수 없는 이벤트 처리
- 세 대표 시나리오 E2E와 추가 질문 없음

추가 검증 대상에는 prompt injection, 모순된 입력, 희소 입력, 혼합 위험 태스크, 잘못된 구조화 출력, 모든 후보 거절, 후보가 없는 지역과 도구 실패가 포함된다. 커버리지 도구가 있는 영역은 의미 있는 assertion으로 80% 이상을 유지한다.

## 완료 기준

- 실제 OpenAI 호출이 핵심 실행 경로에 있다.
- 최초 제출 후 최종 결과까지 요청자에게 추가 입력을 요구하지 않는다.
- 세 대표 시나리오가 라이브로 동작한다.
- 거절·타임아웃 후 `plan.updated`가 다음 행동보다 먼저 발생한다.
- 위험 태스크가 안전 태스크의 성공을 취소하지 않는다.
- 후보 점수 네 구성 요소가 화면에 표시된다.
- `AgentEvent`와 `RawToolEvent`가 분리되어 있다.
- raw provider payload가 변경되지 않는다.
- 모든 데모 데이터와 기능의 simulation 여부가 명확히 표시된다.
- 같은 seed가 같은 정규화 이벤트와 결과를 만든다.
- Replay와 live 실행이 명확히 구분된다.
- 요청 설명과 도구 결과가 프롬프트 지시로 실행되지 않는다.
- 구조화 출력과 모든 API·도구 경계가 스키마로 검증된다.
- 세 시나리오 E2E와 같은 seed의 결정론 테스트가 통과한다.
- 수행한 검증과 미실행 검증, 남은 simulation 범위와 데모 위험을 기록한다.

구현 및 협업 규칙은 저장소의 규칙 문서를 따른다.
