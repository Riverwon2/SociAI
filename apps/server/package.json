{
  "name": "@30-minute-exchange/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "plan:fixture": "tsx src/index.ts",
    "pretest": "pnpm --filter @30-minute-exchange/contracts build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "pnpm --filter @30-minute-exchange/contracts build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@30-minute-exchange/contracts": "workspace:*",
    "openai": "^5.23.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0"
  }
}
