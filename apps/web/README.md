# Web

3번 담당자의 프론트엔드 소유 영역이다.

- `request/`: 한 번에 제출하는 요청 입력
- `timeline/`: 정규화된 `AgentEvent` 화면
- `raw-console/`: 별도 화면의 `RawToolEvent` 원본 콘솔
- `results/`: 태스크별·전체 최종 결과

Replay와 simulation은 라이브 실행과 명확히 구분한다.
