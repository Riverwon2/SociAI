# Contracts

3번 담당자가 기술적으로 관리하고 세 명 모두 변경을 승인하는 공용 계약 영역이다.

Zod 스키마를 런타임 검증과 TypeScript 타입의 단일 원본으로 사용한다. 우선 잠글 계약은 `InitialRequest`, `Task`, `SafetyDecision`, `SufficiencyDecision`, `Candidate`, `AgentEvent`, `RawToolEvent`, `FinalResult`다.
