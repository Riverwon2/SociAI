확인했습니다. 3명 역할은 아래처럼 나누면 A의 중복을 최소화할 수 있습니다.

| 인원 | 역할 | 핵심 책임 |
|---|---|---|
| 1번 | A — AI·오케스트레이션 | OpenAI, 실행 계획, 상태 관리, 재계획 |
| 2번 | B — 결정·검증 | 안전 정책, 후보 랭킹, 시뮬레이터, 단위 테스트 |
| 3번 | A+C — 통합·프론트엔드 | 이벤트 전송, 공용 계약, UI, E2E, 데모 |

### 1번 — A: AI·오케스트레이션 백엔드

- 실제 OpenAI API/SDK 연동
- 요청 해석 및 작업 분해
- 실행 상태 머신과 태스크 관리
- 2번이 만든 결정 도구 호출
- 후보 거절·타임아웃·도구 실패 후 재계획
- `plan.updated` 생성
- 태스크별 결과 및 최종 결과 집계
- OpenAI 통합 테스트와 프롬프트 eval

담당 범위: “에이전트가 어떤 순서로 무엇을 실행할지 결정하는 코어”

### 2번 — B: 결정 엔진·검증

- 합성 요청자·후보 데이터
- 작업별 안전 판정
- 정보 충분성 검사
- 후보 필터링과 점수 계산
- 고정 시드와 가상 시계
- 거절·무응답·수락 시뮬레이터
- 최대 3명, 후보당 가상 10분 정책
- 안전·점수·재시도·결정론 단위 테스트
- 커버리지와 오류 시나리오 관리

담당 범위: “LLM에 맡기면 안 되는 결정론적 정책과 검증”

### 3번 — A+C: 통합·프론트엔드·데모

A 영역:

- `packages/contracts` 기술 관리
- 실행 요청 API와 프론트엔드 연결
- `AgentEvent` 스트리밍
- `RawToolEvent` 원본 전송
- `sequence`와 `toolCallId` 상관관계
- 이벤트 저장·조회와 replay 연결

C 영역:

- one-shot 요청 화면
- 작업·후보·안전 상태 표시
- 후보 점수 구성 표시
- `plan.updated` 타임라인
- raw 이벤트 두 번째 화면
- `simulation` 표시
- 세 대표 시나리오 E2E
- 발표 및 라이브 데모 운영

담당 범위: “백엔드 실행을 심사위원이 이해할 수 있는 라이브 화면으로 연결”

## 주요 연결 관계

```text
1번: 실행 계획과 도구 호출
          ↓
2번: 안전·랭킹·응답 결과 반환
          ↓
1번: 재계획과 최종 결과 결정
          ↓
3번: 이벤트 전송·화면 표시·E2E 검증
```

공용 계약 변경은 3번이 작업하되 세 명 모두 승인하고, 테스트 소유권은 다음과 같이 둡니다.

- 1번: OpenAI·오케스트레이션 통합 테스트
- 2번: 정책·시뮬레이터 단위 및 결정론 테스트
- 3번: 계약·이벤트·E2E 및 데모 테스트



네. 병렬 협업을 시작하기 전에 최소한 아래 내용을 합의해야 합니다. 가장 중요한 것은 공용 계약과 각 담당자의 입출력 경계입니다.

## 1. 공용 계약 먼저 확정

3번이 `packages/contracts`를 관리하되 변경은 세 명 모두 승인합니다.

반드시 먼저 확정할 스키마:

- `InitialRequest`
- `Task`
- `SafetyDecision`
- `Candidate`
- `AgentEvent`
- `RawToolEvent`
- `FinalResult`

공통 규칙:

- 모든 이벤트에 `runId`, `requestId`, `sequence`
- 작업 이벤트에는 `taskId`
- 도구 호출과 결과에는 같은 `toolCallId`
- `RawToolEvent.raw`는 절대 수정하지 않음
- 계약 변경 시 기존 필드 삭제·이름 변경 금지
- 예제 JSON과 계약 테스트도 함께 수정

## 2. 담당자 간 인터페이스

| 제공자 | 소비자 | 먼저 합의할 내용 |
|---|---|---|
| 3번 → 1번 | 실행 요청 API | 검증된 `InitialRequest` 전달 방식 |
| 1번 → 2번 | 결정 도구 호출 | 안전 판정·랭킹·시뮬레이터 입출력 |
| 2번 → 1번 | 결정 결과 | 오류, 후보 없음, 타임아웃 표현 방식 |
| 1번 → 3번 | 실행 이벤트 | 이벤트 순서와 `plan.updated` 구조 |
| 1번 → 3번 | OpenAI raw 이벤트 | 원본 payload 전달 및 상관관계 |

2번의 기능은 가능하면 다음과 같은 순수 함수로 제공해야 합니다.

- `checkSafety(task) → SafetyDecision`
- `rankCandidates(task, candidates) → RankedCandidate[]`
- `simulateResponse(scenario, candidate) → ResponseEvent`
- `aggregateTaskResults(results) → FinalResult`

## 3. 공용 시나리오 fixture

동일한 데이터로 개발하도록 세 가지 fixture를 먼저 만듭니다.

1. 첫 후보 수락 → `fully_matched`
2. 거절 → 타임아웃 → 세 번째 수락 → `fully_matched`
3. 문서 전달 성공 + 약 복용 보조 차단 → `partially_matched`

각 fixture에는 입력, 후보 목록, 응답 순서, 예상 이벤트 순서, 최종 결과가 포함되어야 합니다. 실명이나 실제 주소는 사용하지 않습니다.

## 4. 파일 소유권

- 1번: OpenAI, orchestration, run/task 상태, 결과 집계
- 2번: safety, ranking, simulator, synthetic fixtures
- 3번: contracts, event transport, frontend, E2E
- 공용 설정·계약 파일: 수정 전에 단체 채널에 알림

다른 사람의 소유 파일은 직접 대규모 수정하지 말고 인터페이스 변경 요청으로 처리합니다.

## 5. 통합 체크포인트

- 체크포인트 1: 계약과 fixture 확정
- 체크포인트 2: 시나리오 1을 처음부터 화면까지 연결
- 체크포인트 3: 시나리오 2·3 통합
- 체크포인트 4: 실제 OpenAI와 raw 두 번째 화면 검증
- 체크포인트 5: 전체 테스트와 라이브 리허설

해커톤에서는 마지막에 한 번 합치지 말고, 2~3시간 간격으로 통합하는 편이 안전합니다.

## 6. 공통 완료 기준

- 요청 도중 추가 질문 없음
- 실제 OpenAI 호출이 핵심 경로에 존재
- `plan.updated`가 다음 행동보다 먼저 발생
- raw payload가 변경되지 않음
- 위험 작업이 안전한 작업의 성공을 취소하지 않음
- 세 시나리오 E2E 통과
- 동일 seed에서 동일한 정규화 이벤트 발생
- simulation 기능이 화면에 명확히 표시됨
- 80% 이상 테스트 커버리지
- 커밋 직전 `2026-08-01 14:00 KST` 이후 여부 확인

병렬 작업의 실질적인 시작 조건은 “계약 + 세 시나리오 fixture + 이벤트 순서”가 확정된 상태입니다. 이 세 가지가 없으면 각자 구현은 가능해도 통합 단계에서 다시 만들 가능성이 큽니다.